import { SkChat } from "@/components/skeleton";

/** Salas de chat por plan. */
export default function Loading() {
  return (
    <main className="pb-20 pt-6 md:pb-28 md:pt-10">
      <SkChat />
    </main>
  );
}
